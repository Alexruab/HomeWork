#include <stdio.h>
#include <math.h>

double a, Sn, i, j;

int main()
{
	scanf_s("%Lf", &a);

	Sn = 0;

	i = 1;

	while (Sn < a)
	{
		Sn += 1 / i;
		i = i * 2;
		j = j + 1;
	}

	printf("%lf", j);

	return 0;
}
